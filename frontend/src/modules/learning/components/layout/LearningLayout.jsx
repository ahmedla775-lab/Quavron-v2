export default function LearningLayout({ children }) {

  return (

    <div
      className="min-h-screen"
      style={{
        background: "var(--q-background)",
      }}
    >

      {children}

    </div>

  );

}
