[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "quavron-intelligence"
version = "0.1.0"
description = "Independent intelligence and reasoning engine for the Quavron ecosystem"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
